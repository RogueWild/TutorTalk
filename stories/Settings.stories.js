import React from 'react';
import Settings from '../comps/Settings';

export default {
    title: 'Settings/Settings Form', 
    component: Settings
};

export const BasicSettings = () => <Settings />;
